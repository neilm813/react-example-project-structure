/* 
If you needed to convert between many units, you would pick a base unit to use
for conversions so only the base unit needs to be convertible to all others.
*/

/**
 * Converts kilometers to miles.
 * @param {number} km Kilometers
 * @returns {number} Miles
 */
export const kmToMi = (km) => km * 0.621371;
